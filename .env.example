# api-tests configuration
# Copy to .env and fill in values

# Backend base URLs (no trailing slash)
WEATHER_API_URL=http://localhost:8080
RACES_API_URL=http://localhost:8081

# Shared internal key — must match INTERNAL_API_KEY in weather-api/.env and races-api/.env
INTERNAL_API_KEY=
